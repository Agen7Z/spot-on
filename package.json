{
  "name": "spot-on-period-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Spot On — MERN-based period tracker with cycle prediction, reminders, and dashboard.",
  "scripts": {
    "dev": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "lint": "echo \"No root lints configured\""
  },
  "workspaces": [
    "client",
    "server"
  ]
}
